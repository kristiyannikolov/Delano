{% extends "::base.html.twig" %}

{% block title %}Delano / Reservations{% endblock %}
{% block body %}
    <div class="container">
    {% for flashMessage in app.session.flashbag.get('reserve') %}
         {{ flashMessage }}
        {% endfor %}
    <div class="row">
        <div class="col-lg-5">
        <div class="panel panel-primary">
            <div class="panel-heading"><h2 class="panel-title">Make a Reservation</h2></div>
<div class="panel-body">

        <form class="form-group" method="post">
        {{ form_widget(rq) }}
        {{ form_rest(rq) }}
        <div class="control-group">
            <div class="controls">
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </div>     
    </form>
            </div>
        </div>

        </div>

        <div class="col-xs-3">

            <img src="{{ asset('img/asdfrg.jpg') }}" class="img-thumbnail img-responsive" />
    </div>
    </div>
    </div>
{% endblock %}